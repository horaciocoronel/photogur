<!DOCTYPE html>
<html>
  <head>
    <title>Add a Picture</title>
  </head>
  <body>
    <h1>Add a Picture</h1>
    <%= link_to("Back to Pictures", "/pictures") %>
    <%= form_for(@picture) do |f| %>
    <p>
      <%= f.label :artist %><br>
      <%= f.text_field :artist %>
    </p>
    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </p>
    <p>
      <%= f.label :url %><br>
      <%= f.text_field :url %>
    </p>
    <p>
      <%= f.submit "Save" %>
    </p>
    <% end %>

    <p>
      <% if @picture.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@picture.errors.count, "error") %> prohibited this Picture from being saved:</h2>

    <ul>
    <% @picture.errors.values.each do |msg| %>
       <% msg.each do  |m| %>
      <li><%= m %></li>
    <% end %>
    <% end %>
    </ul>
  </div>
<% end %>
    </p>
  </body>
</html>
