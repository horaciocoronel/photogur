<!DOCTYPE html>
<html>
  <head>
    <title>One Picture</title>
  </head>
  <body>
    <%= link_to("Back to Pictures", "/pictures") %> | <%= link_to("Edit", "/pictures/#{@picture.id}/edit") %><br>
    <%= @picture.title %>, <%= @picture.artist %></p>
    <img src="<%= @picture.url %>"
  </body>
</html>
